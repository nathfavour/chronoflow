"use client";

import { AppLayout } from "@/newui/components/AppLayout";
import { EnhancedSettings } from "@/newui/components/EnhancedSettings";

export default function SettingsPage() {
  return (
    <AppLayout>
      <EnhancedSettings />
    </AppLayout>
  );
}